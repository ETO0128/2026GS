# C 题建模方案与团队分工

本文档整理团队当前关于 2026 年数学建模竞赛 C 题“微网与外部电网电力调控策略”的讨论结论，用于统一模型定义、开发接口和分工。后续发现题意理解或实验结果需要调整时，应先更新本文档，再修改各分支代码。

## 一 选题与总体路线

团队最终选择 C 题，不考虑 A 题。C 题适合采用优化与强化学习结合的方法，四个问题可以沿同一套微网能量管理框架逐步扩展。

当前确定的主线是：

1. 用线性规划建立确定性最优调度基准；
2. 用时间序列模型预测负荷、光伏和必要时的电价；
3. 用滚动优化处理预测更新和购电计划调整；
4. 用强化学习学习预测误差下的安全储备与实时修正；
5. 比较规则策略、线性规划、滚动优化、强化学习和混合策略。

不建议从第一问开始全部使用纯强化学习。第一问的确定性线性规划能够提供可解释的最优基准，也可以用于检查强化学习环境和费用计算是否正确。强化学习的主要价值应体现在问题二至问题四的不确定环境中。

## 二 四个问题的逻辑关系

```text
公共数据处理与储能模型
          │
          ├── 问题一：确定性单日优化
          │
          ├── 问题二：预测误差与紧急购电
          │        └── 问题四的 4-2：波动电价下重做问题二
          │
          └── 问题三：滚动预测与调整购电
                   └── 问题四的 4-3：波动电价下重做问题三
```

问题一的具体数值结果不是后续问题的前置条件，但问题一建立的能量平衡、储能状态转移、单位换算和优化接口必须被后续问题复用。

问题二和问题三可以在公共模型确定后并行开发。问题四不能独立完成，它分别是问题二和问题三在波动电价条件下的扩展。

## 三 数据结构

C 题数据位于 [`problems/C题`](problems/C题)。

- 附件 1：一天 144 个十分钟时段的电价、小区负荷和光伏预测功率；
- 附件 2：2025 年 365 天的小区实际负荷和光伏实际功率，每天 144 个时段；
- 附件 3：每天在 0:00、6:00、12:00、18:00 发布的未来 24 小时整点光伏预测；
- 附件 4：2025 年 365 天的十分钟级波动电价；
- 附件 5：五个问题结果文件的 Excel 模板。

所有原始数据均未发现缺失值。结果模板从 2025 年 2 月 1 日开始，因此当前采用以下因果决策原则：

- 2025 年 1 月作为初始训练和校准期；
- 2 月 1 日至 12 月 31 日逐日决策和回测；
- 某个决策时刻不得使用该时刻之后才会产生的实际数据；
- 模型可在每天结束后吸收当天实际数据，并采用扩展窗口更新。

如果最终决定采用不同的信息假设，必须在论文中明确说明，并同时给出不会偷看未来数据的对照实验。

## 四 统一数学模型

### 4.1 时间和单位

每个时间步为 10 分钟：

\[
\Delta t=\frac{1}{6}\text{ h}
\]

附件中的负荷和光伏是功率，单位为 kW。优化变量和结果模板中的购电量、充放电量使用 kWh，因此：

\[
l_t=L_t\Delta t,\qquad v_t=P_t^{PV}\Delta t
\]

### 4.2 决策变量

- \(g_t\)：外网购电量，kWh；
- \(c_t\)：从微网交流母线送入储能的充电量，kWh；
- \(d_t\)：储能向交流母线提供的放电量，kWh；
- \(e_t\)：储能内部储电量，kWh；
- \(w_t\)：无法消纳的弃光量，kWh；
- \(g_t^{emg}\)：紧急购电量，kWh；
- \(a_t^+,a_t^-\)：向上和向下调整的购电量，kWh。

充放电量的定义必须在所有模块中保持一致。当前规定 \(c_t\) 和 \(d_t\) 都是交流母线侧电量，效率只在储能状态转移方程中计算。

### 4.3 能量平衡

\[
g_t+g_t^{emg}+v_t-w_t+d_t=l_t+c_t
\]

不允许向外网售电，因此 \(g_t\ge0\)。光伏多余时优先充电，仍无法消纳的部分计入 \(w_t\)。

### 4.4 储能状态转移

\[
e_{t+1}=e_t+\eta_c c_t-\frac{d_t}{\eta_d}
\]

其中：

\[
\eta_c=\eta_d=0.9
\]

储能约束：

\[
1200\le e_t\le10800
\]

\[
0\le c_t,d_t\le5000\times\frac{1}{6}=833.33
\]

2025 年 1 月 1 日 0:00 的初始储电量为 6000 kWh。全年模型中每天 24:00 的储电量必须传递为第二天的初始储电量。

在电价非负、无充放电补贴且效率小于 1 时，同时充放电通常不会成为线性规划的最优解。如果实验发现同时充放电，需要加入互斥约束或二进制变量，将模型改为混合整数线性规划。

## 五 各问题的方案

### 5.1 问题一 确定性单日优化

目标函数：

\[
\min \sum_{t=1}^{144}p_tg_t
\]

使用附件 1 的已知电价、负荷和光伏预测，求一天内每十分钟的购电量和储能调度。

需要重点处理：

- 输入功率乘 \(1/6\) 转换为电量；
- 输入第一行是 00:10，最后一行是下一天 0:00，必须逐行核对结果模板的区间；
- 汇总题目指定的购电时段和四小时充放电量；
- 记录 0:00 与 24:00 的储电量；
- 检查每个时段的能量平衡和储能上下界。

日末储电量会产生截断效应。如果完全不设置终端条件，模型可能在最后时刻把电池放到下限以获得一次性低成本。应比较以下方案，并在论文中说明最终选择：

- \(e_{144}=e_0\)；
- \(e_{144}\ge e_0\)；
- 为日末剩余电量设置终端价值；
- 使用超过 24 小时的滚动优化窗口，只执行第一天决策。

### 5.2 问题二 预测误差与紧急购电

每天 0:00 根据历史信息预测当日负荷和光伏，再用公共优化器生成购电和储能计划。实际供能不足时，缺口按照当时正常电价的 5 倍紧急购电。

建议建立以下流程：

```text
历史数据
  → 当日负荷和光伏预测
  → 确定性基础调度
  → 实际负荷和光伏逐步揭示
  → 储能修正与紧急购电
  → 当日费用和状态记录
  → 加入当天数据后更新模型
```

基础目标函数可写为：

\[
\min\mathbb{E}\left[
\sum_t p_tg_t+5p_tg_t^{emg}
\right]
\]

可先完成以下基准：

1. 前一日同一时刻预测；
2. 最近若干天同一时刻均值；
3. 同星期类型的滚动预测；
4. 线性规划加固定安全储备；
5. 场景优化或强化学习策略。

主要难点是预测模型不能使用未来实际数据。1 月只有 31 个初始训练日，强化学习样本不足，需要利用具有日内相关性的预测残差生成训练场景，而不是对每个时间步独立添加高斯噪声。

### 5.3 问题三 滚动预测与计划调整

每天 0:00 生成基础计划。在 6:00、12:00、18:00 收到新光伏预测后，只对尚未执行的时间段重新优化。

建议使用模型预测控制：

```text
收到新预测
  → 更新未来净负荷
  → 固定已经执行的决策
  → 优化剩余时段
  → 记录调整购电量和费用
  → 执行最近一个时间步
```

计划更新关系：

\[
q_t^{new}=q_t^{old}+a_t^+-a_t^-
\]

向下调整部分不收费，向上调整部分按照交易时刻电价的 1.5 倍计费。紧急购电仍按照 5 倍电价计费。

主要难点：

- 附件 3 是小时级预测，控制模型是十分钟级，需要非负且平滑的插值；
- 未来 24 小时预测可能跨越当天边界；
- 多次调整必须保存计划版本，避免费用重复计算；
- 需要明确调增量相对最初计划还是上一次计划计算；
- 需要实验比较 12、6、3、1 小时等更新频率，分析增加预测时刻是否值得。

### 5.4 问题四 波动电价

问题四分为两个部分：

- `result4-2.xlsx`：将问题二的固定日内电价替换为波动电价；
- `result4-3.xlsx`：将问题三的固定日内电价替换为波动电价。

主要难点是题目没有完全说明制定计划时是否已经知道未来全天实时电价。建议同时构造：

- 完全知道未来电价的理想下界；
- 只使用当前和历史价格的因果策略。

如果正式结果采用提前知道全天电价的假设，必须在论文中明确说明。更有说服力的强化学习实验应使用因果策略，并把未来电价预测误差加入环境。

## 六 强化学习方案

优先考虑适用于连续动作空间的 SAC，也可以使用 PPO 或 TD3 作为对照。当前推荐优化与强化学习混合：

\[
a_t=a_t^{LP}+\Delta a_t^{RL}
\]

其中线性规划或滚动优化给出基础调度，强化学习只学习储能动作、安全储备或购电计划的修正量。

### 6.1 状态

状态可以包含：

- 当前时间的正余弦编码；
- 当前储能电量；
- 当前电价和未来电价特征；
- 当前及未来负荷预测；
- 当前及未来光伏预测；
- 最近若干时段的预测误差；
- 原计划购电量和已调整购电量；
- 距离下一次预测发布时间；
- 当日累计费用与紧急购电量。

不建议把未来 144 个原始数值全部直接输入一个普通多层感知机。可以使用未来 1、3、6、12、24 小时的均值、峰值和累计净负荷等压缩特征，或者使用专门的序列编码器。

### 6.2 动作

优先让智能体输出一个有符号的储能功率或相对基础计划的修正量：

- 正值表示充电；
- 负值表示放电；
- 购电量由能量平衡自动计算。

不要让智能体分别自由输出购电、充电和放电三个互相耦合的动作，否则训练更难，也更容易违反约束。

### 6.3 奖励

奖励以实际费用为核心：

\[
r_t=-p_tg_t-1.5p_ta_t^+-5p_tg_t^{emg}
\]

可以加入较小的电池动作平滑项，但不应使用任意的大惩罚替代真实费用。容量、功率和供电平衡应由环境中的安全投影层强制保证。

### 6.4 训练和验证

- 使用 1 月初始化预测模型和策略；
- 使用扩展时间窗口进行在线更新；
- 从历史预测残差中按季节、星期类型和时段进行块采样；
- 使用连续时间段划分训练集、验证集和测试集；
- 禁止随机打乱全年数据后再划分，否则会泄漏季节和未来信息；
- 对比规则策略、线性规划、滚动优化、纯 SAC 和混合策略。

## 七 统一评价指标

所有方法至少比较：

- 总购电费用；
- 正常计划购电费用；
- 调整购电费用；
- 紧急购电费用；
- 紧急购电量；
- 总购电量；
- 弃光量和光伏消纳率；
- 储能利用率；
- 日末和全年末储电量；
- 约束违反次数；
- 相对完全信息最优解的费用差距；
- 计算时间和强化学习训练稳定性。

预测模型还应报告 MAE、RMSE，并分别统计负荷、光伏和电价预测误差。

## 八 团队分工

当前建议按问题和公共模块结合分工：

| 分支 | 主要任务 | 结果文件 |
| --- | --- | --- |
| `cmx` | 公共数据规范、储能优化内核、问题一 | `result1.xlsx` |
| `xtc` | 负荷和光伏预测、问题二、问题四的 4-2 | `result2.xlsx`、`result4-2.xlsx` |
| `lcy` | 滚动预测与调整、问题三、问题四的 4-3 | `result3.xlsx`、`result4-3.xlsx` |

问题四不应由一人单独负责。`4-2` 自然继承问题二，`4-3` 自然继承问题三。

## 九 公共代码接口

建议公共优化器提供以下接口：

```python
solve_schedule(
    price,
    load_forecast,
    pv_forecast,
    initial_soc,
    fixed_plan=None,
) -> schedule
```

返回值至少包含：

```python
{
    "grid_purchase": ...,    # 计划购电量
    "charge": ...,          # 母线侧充电量
    "discharge": ...,       # 母线侧放电量
    "soc": ...,             # 储能内部电量
    "curtailment": ...,     # 弃光量
    "cost": ...,
}
```

实际运行仿真接口：

```python
simulate_day(
    schedule,
    actual_load,
    actual_pv,
    actual_price,
) -> result
```

返回值至少包含正常购电、调整购电、紧急购电、实际充放电、储能轨迹、总费用和约束检查结果。

推荐目录：

```text
src/
├── data/          # 数据读取、清洗和时间索引
├── forecast/      # 负荷、光伏和电价预测
├── optimization/  # 线性规划、场景优化和滚动优化
├── rl/            # 强化学习环境、训练和策略
├── evaluation/    # 指标、对比实验和绘图
└── outputs/       # 结果模板填写逻辑
```

## 十 开工前必须统一的定义

以下问题不能由三个人分别决定：

- 输入时间表示区间起点还是终点；
- 功率转电量是否统一乘 \(1/6\)；
- 充电量和放电量位于储能侧还是交流母线侧；
- 日末储电量如何处理，是否跨天连续；
- 决策时允许使用哪些未来数据；
- 紧急购电前是否允许临时改变储能动作；
- 调整费用相对最初计划还是上一次计划计算；
- 小时级光伏预测采用何种十分钟插值；
- 同时充放电如何禁止或验证不会出现；
- 结果保留多少小数以及 Excel 模板如何填写。

这些定义确定后，应写成公共配置和测试，而不是分散在三份代码中。

## 十一 推荐开发顺序

1. 共同确定时间索引、单位、信息边界和费用口径；
2. 完成统一数据加载器和输入校验；
3. 完成问题一线性规划和公共优化接口；
4. 完成全年逐日仿真器和规则策略基准；
5. 并行开发问题二的预测误差策略与问题三的滚动调整；
6. 将问题二切换至波动电价得到 4-2；
7. 将问题三切换至波动电价得到 4-3；
8. 在已有完整基准结果后训练 SAC；
9. 完成消融实验、敏感性分析和图表；
10. 自动填写并检查五个结果工作簿，再开始论文最终整合。

阶段性原则是先保证四个问题都有可运行、可解释的基准结果，再增加强化学习创新。这样即使强化学习效果不理想，项目仍然能够形成完整论文和合格结果文件。
